class user {
  int id;
  String email;
  String firstName;
  String lastName;
  String username;
  int? password;

  user(this.id, this.email, this.firstName, this.lastName, this.username,
      this.password);
}
